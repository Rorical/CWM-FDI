[project]
name = "assignment6"
version = "0.1.0"
description = "Carbon footprint estimator for a pair of client and server connections."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
